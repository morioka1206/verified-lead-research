# ADR 003: ブラウザと保存

## 決定

静的HTMLを先に取得し、JavaScriptが必要な場合だけローカルPlaywrightを使う。Bot対策は突破せず `blocked` とする。

最終CSVは日本語だけにし、監査JSONには元サイトの証拠文を残す。実行結果は `runs/` に保存してGit管理しない。

## 理由

取得コストとサイト負荷を抑えながら、JavaScriptサイトを確認するため。日本語での読みやすさと、原文照合による検証可能性を両立するため。
